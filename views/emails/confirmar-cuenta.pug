doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
            
    body 
        h2 Confirma tu cuenta Teslera
        p Ya casi estás inscrito, solo presiona el siguiente botón.
        a(href=`${confirmarURL}`) Confirmar cuenta
        p Si no puedes aceder a este enlace, visita #{confirmarURL}