<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta Teslera

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                  
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") Tu nombre:
                    input(type="text" name="nombre" placeholder="Tu nombre" value= nombre)
                .campo
                    label(for="email") Correo electronico:
                    input(type="email" name="email" placeholder="Tu correo" value= email)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Tu contraseña")
                .campo.enviar
                    input.boton(type="submit" value="Crear cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar sesión
                    
        script(src="/dist/bundle.js")