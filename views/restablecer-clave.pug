doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina
            p Agrega tu correo para restablecer la contraseña

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                                
            form.caja-login(method="POST" action="/restablecer-clave")
                .campo
                    label(for="email") Correo electronico:
                    input(type="email" name="email" placeholder="Tu correo" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer contraseña")
                .campo.acciones 
                    a(href="/iniciar-sesion") Volver a iniciar sesión 